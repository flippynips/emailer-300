
/** Represents an account. */
export interface Account {
  
  /** Username of the account. */
  Username: string;
  /** Token used for succeeding requests. */
  Token: string;
  /** Sender email address of the account. */
  Sender: string;
  
}
